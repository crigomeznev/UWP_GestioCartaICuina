<UserControl
    x:Class="GestioComandes.View.UIComanda"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:GestioComandes.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400"
    x:Name="ucComanda">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>

        <TextBlock Text="COMANDA" Grid.Row="0"></TextBlock>

        <StackPanel Grid.Row="1" Orientation="Vertical" Padding="10">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Taula " FontSize="20"></TextBlock>
                <TextBlock Text="{Binding ElementName=ucComanda, Path=PComandaDB.Taula}" FontSize="20" FontWeight="Bold"></TextBlock>
            </StackPanel>
            <StackPanel Orientation="Vertical">
                <!-- todo converter perque nomes es vegi hora de comanda -->
                <TextBlock Text="{Binding ElementName=ucComanda, Path=PData}"></TextBlock>
                <TextBlock Text="{Binding ElementName=ucComanda, Path=PComandaDB.Cambrer.Nom}"></TextBlock>
            </StackPanel>
        </StackPanel>


        <!--línies de comanda-->
        
        
        
        <ListView ItemsSource="{Binding ElementName=ucComanda, Path=PComandaDB.Linies}" Grid.Row="2" Background="Yellow">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <local:UILiniaComanda></local:UILiniaComanda>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

    </Grid>
</UserControl>
